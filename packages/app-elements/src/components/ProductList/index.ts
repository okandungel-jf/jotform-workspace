export { ProductList } from './ProductList';
export type { ProductListProps, ProductListLayout, ProductItem, ProductOptionDimension, ProductVariant, ProductModifier, ProductModifierFieldType } from './ProductList';
export {
  generateVariants,
  ensureProductIds,
  makeDimensionId,
  variantLabel,
  MAX_DIMENSIONS,
  MAX_VALUES_PER_DIMENSION,
} from './types';
