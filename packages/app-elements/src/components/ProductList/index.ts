export { ProductList } from './ProductList';
export type { ProductListProps, ProductListLayout, ProductItem, ProductOptionDimension, ProductOptionChoice, ProductVariant, ProductModifier, ProductModifierFieldType, ProductModifierChoice, ProductSubscription, ProductSubscriptionRepeatUnit } from './ProductList';
export {
  generateVariants,
  ensureProductIds,
  makeDimensionId,
  variantLabel,
  buildVariantId,
  MAX_DIMENSIONS,
  MAX_VALUES_PER_DIMENSION,
} from './types';
