---
category: Builder Chrome
---

Builder Chrome — real app-builder shell UI (top bar, properties panel, add-element
panel) shown for reference. These are composed from the design-system primitives;
they are the builder's own UI, not reusable canvas components.
